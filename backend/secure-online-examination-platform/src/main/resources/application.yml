spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/soep_db
    username: postgres
    password: Jameer@222

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: never